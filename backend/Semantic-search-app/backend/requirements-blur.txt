# GPU blur overlay — install on Kaggle GPU notebooks:
# pip install -r requirements.txt -r requirements-blur.txt
# pip uninstall -y onnxruntime && pip install onnxruntime-gpu  # InsightFace GPU
ultralytics>=8.0.0
insightface>=0.7.3
onnxruntime-gpu>=1.16.0
